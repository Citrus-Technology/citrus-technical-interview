import { ProgramNode } from '../interfaces/program-node'

export const findNode = (
  node: ProgramNode,
  id?: string
): ProgramNode | undefined => {
  /* 
   * TODO: Search the tree to find the program location node
   *
   * 1. If the node cannot be found, return undefined
   * 2. If the id is undefined return the parent node
   * 3. Find the node that matches the id within the program tree
  */
  return undefined
}
